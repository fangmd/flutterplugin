name: p_install
description: app upgrade
version: 0.0.4
homepage: https://github.com/fangmd/flutterplugin

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  plugin:
    androidPackage: com.passon.p_install
    pluginClass: PInstallPlugin
